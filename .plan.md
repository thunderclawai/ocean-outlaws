# Plan: GLB asset loader: GLTFLoader + DRACOLoader replacing FBXLoader

## Issue
#117 — Replace FBX model loading with GLB/Draco-compressed models via GLTFLoader + DRACOLoader

## Analysis

The bulk of this migration is already complete across earlier commits on this branch (cb39a0d and prior). Here's the current state:

### Already Done
- **GLTFLoader + DRACOLoader** is the sole model loader (`game/js/glbVisual.js`), FBXLoader fully removed
- **`fbxVisual.js` renamed to `glbVisual.js`** — exports `loadGlbVisual()` with DRACOLoader singleton, template caching, `fitToSize()`, and `applyFlat()` (MeshToonMaterial conversion)
- **All 232 GLB files** exist under `game/assets/models/` (ships, environment, houses, islands, trees, stones, mountains, lands, vehicles, plants, waters, ships-palmov)
- **All FBX files removed from `game/assets/models/`** — 0 FBX files tracked in git
- **`manifest.json`** updated — all ship paths point to `.glb`
- **Compositions** (`palmov-30.json`, `ports-islands-20.json`) updated with `.glb` paths
- **MeshToonMaterial** applied via `applyFlat()` in `glbVisual.js` (line 32-50) — converts all loaded materials to toon with boosted saturation
- **Conversion script** at `scripts/convert-fbx-to-glb.sh` — documented, uses FBX2glTF + gltf-transform draco
- **Palmov Island source FBX files** are gitignored and not tracked (239 files on disk, 0 in git)
- **Ship loading flow**: `artOverrides.js` reads manifest → `ship.js`/`enemy.js` call `getOverridePath()` → `loadGlbVisual()` handles load + material conversion
- **Boss loading** in `boss.js` also updated to use GLB paths

### Remaining Work
The only remaining item is updating **`ASSETS.md`** documentation, which still shows stale FBX references:
- Directory tree shows `.fbx` extensions (lines 9-16)
- Naming conventions reference `.fbx` (lines 48-52)
- Export pipeline describes Unity/UnityGLTF workflow instead of the FBX2glTF pipeline (lines 65-73)
- Manifest example shows `.fbx` paths (lines 100-107)
- "What Belongs" section still mentions FBX (line 125)
- "Source Packs" says Emil extracts from Unity (line 145) — now automated via script

## Changes

### ASSETS.md (modify)
- **Lines 6-43 (Directory structure)**: Update file extensions from `.fbx` to `.glb` throughout the tree. Update directory names to match actual structure (`environment/`, `houses/`, `trees/`, etc. — not `structures/`, `nature/`, `props/`, `effects/`)
- **Lines 46-52 (Naming conventions)**: Change `.fbx` to `.glb` in all examples (`sloop.glb`, `palm-tree-1.glb`, etc.)
- **Lines 54-61 (File formats table)**: Already says `.glb` — keep as-is
- **Lines 63-73 (Export pipeline)**: Replace Unity/UnityGLTF pipeline with the FBX2glTF + gltf-transform pipeline. Reference `scripts/convert-fbx-to-glb.sh`. Remove Unity-specific steps.
- **Lines 81-90 (Runtime loader snippet)**: Update Draco decoder path to match actual code (`https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/libs/draco/` per `glbVisual.js:11`)
- **Lines 96-118 (Manifest example)**: Update all `.fbx` paths to `.glb`, update to match current manifest.json entries (sloop, destroyer, brigantine, cruiser, galleon, carrier, manowar, submarine, enemy_patrol, boss_battleship, boss_carrier)
- **Lines 122-137 (What Belongs)**: Remove "FBX" from include list (line 125), just say "GLB model files". Clarify that raw FBX source packs are gitignored (not in a separate repo).
- **Lines 139-145 (Source Packs)**: Update to say raw Palmov packs are gitignored at `game/assets/Palmov Island/` and conversion is automated via `scripts/convert-fbx-to-glb.sh`. Remove reference to Emil manually extracting from Unity.

No other code files need changes — the runtime migration is complete.

## Order of Operations
1. Update `ASSETS.md` — all sections listed above, in order from top to bottom
2. Verify no stale FBX references remain in any tracked files (grep for `.fbx` excluding `.gitignore` and `scripts/`)
3. Commit, push, open draft PR

## Testing
- `grep -r '\.fbx' game/js/` should return 0 results (already passes)
- `git ls-files '*.fbx'` should return 0 results (already passes)
- All runtime model paths in `manifest.json` should point to `.glb` files that exist on disk
- Game loads in browser without console errors for model loading
- Ships (sloop, brigantine, galleon, manowar) display with MeshToonMaterial aesthetic
- Enemy patrol and boss ships load GLB models
- Composition islands render correctly with GLB environment models
- Mobile load time should be under 2s for initial scene (DRACOLoader + template caching helps)

## Risks
- **ASSETS.md is documentation only** — no runtime risk from updating it
- The actual runtime migration is already in place and presumably tested through the earlier PRs (#172-#175)
- The Palmov Island source FBX files on disk (13MB, gitignored) could confuse future contributors — ASSETS.md update clarifies this
